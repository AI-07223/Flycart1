// Legacy shim — the arcade menu was replaced by the menu.ts screen router.
// Kept only so any stray import keeps resolving to the new system.
export { MENU_HOST_ID as ARCADE_MENU_HOST_ID } from "./menu";
export {};
